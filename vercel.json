{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --force"
      }
    }
  ],
  
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
